services:
  - type: web
    name: scriptovision
    env: python
    plan: pro
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 600 --keep-alive 5
    healthCheckPath: /
    disk:
      name: scriptovision-data
      mountPath: /data
      sizeGB: 10
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: JOBS_DIR
        value: /data/jobs
      - key: OUTPUT_DIR
        value: /data/output
      - key: TEMP_DIR
        value: /data/temp
